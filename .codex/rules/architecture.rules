# 架构卫生规则
#
# 注意：`.rules` 文件会被 Codex 按 Starlark 解析。治理说明必须写成注释，
# 不要写 Markdown 正文，否则会触发 rules 加载失败。
#
# ## 上下文饱和检查
#
# 开始任何非琐碎代码变更前，必须确认待修改区域是否命中以下阈值：
#
# - 单个手写代码文件超过 800 行。Markdown 文档不适用该阈值。
# - 单类超过 30 个公开方法。
# - 单函数超过 80 行，或圈复杂度超过 15。
# - 路由文件超过 20 个路由。
# - 即将复制超过 40 行已有代码。
# - 同一职责在 3 个及以上文件中通过字符串字面量耦合。
# - 同一实体存在多套手写 schema、DTO、ORM 或序列化表示且缺少单一真相来源；Python 项目中包括 Pydantic 模型与手写 dict 双轨表示。
# - 表达持久化生命周期的状态字段（如 `status` / `state` / `phase` / `stage`）的合法转移没有集中定义，或只登记状态集合但缺完整转移表。
# - 跨表、跨文件、跨服务或 DB + 文件系统更新缺少幂等键、回滚策略，或在事务块内执行不可回滚副作用。
# - 旧 facade、兼容 shim、历史路径、旧数据格式、过期 API、不可达 UI 分支或调试脚本仍参与主流程，且没有明确迁移/删除计划。
#
# 任一阈值命中时，计划中必须先说明拆分、抽象、注册表、状态机或 schema
# 单一来源的处理方式；不得以“匹配现有风格”或“检查未报警”为理由继续追加同类债务。
#
# ## 替换旧设计模式
#
# 当用户提出重构、去除旧设计、优化架构、提高代码质量、引入更优秀设计方案/框架/架构，
# 或 Analyze 阶段发现旧债信号时，必须进入替换旧设计模式。
#
# 替换旧设计模式的优先级为：代码质量 > 新设计/框架/架构 > 旧模式兼容或保留。
# 旧实现只有被明确列入保留清单，或属于已确认公开契约、数据迁移边界、安全边界时才保留。
#
# 计划必须逐项说明：
#
# - 删除：旧 facade、兼容 shim、历史路径、重复实现、不可达分支或过期 API 中哪些直接删除。
# - 迁移：公开 API、配置/env、持久化数据、文档、测试和调用方如何迁移到新设计。
# - 保留：哪些旧入口因公开契约、安全、迁移窗口或用户确认而暂时保留，以及后续清理条件。
#
# 不得以“精准改动”“匹配现有风格”“外部行为不变”为理由继续保留已确认应替换的旧设计。
#
# ## 旧设计申辩拦截
#
# 当问题暴露为产品页面空状态、错误详情未投影、重复 job、重复提示词、重复输出 schema、
# DSPy Signature 与提示词双重约束、OpenAPI/前端类型残留旧入口、formatter/normalizer 双轨、
# 单/批次工作流分支重复或历史数据参与主流程时，默认视为设计质量问题。
#
# 分析和计划必须覆盖：
#
# - 产品症状：用户看到了什么、期望是什么、失败状态是否完整投影到 API 和 UI。
# - 证据链：job 状态、error_json/raw_output_json、store 投影、API response、前端 selector/tab 展示。
# - 设计异味：是否存在多入口、多 schema、多 prompt、多 normalizer、多状态来源或历史兼容路径继续参与主流程。
# - 替换边界：哪些旧入口删除，哪些历史数据迁移或清理，哪些兼容点只保留为只读历史解释。
#
# 不得把“旧逻辑本来就是这样”“为了兼容所以保留”“先小修一下”作为默认结论。
#
# ## Typed Output 单一契约
#
# 当 DSPy Signature 的 OutputField 指向 Pydantic 模型，或运行时已经有明确 OutputModel /
# Record / Response model 时，typed model 是默认结构契约。不得在主流程中继续用裸 dict、
# `JsonObject`、`schema_version` 字符串、提示词格式要求或 normalizer 作为并行契约。
#
# 计划必须说明 typed model 的流转边界：
#
# - 内部：formatter、runner、orchestrator、store completion 优先传递 Pydantic model 实例。
# - 边界：SQLite JSON、HTTP response、临时文件、日志和 Langfuse metadata 才允许 `model_dump(mode="json")`。
# - 兼容：normalizer 只用于历史数据、外部脏输入或测试 fixture 迁移；不得参与新主流程。
# - 版本：没有外部协议、多版本运行时、历史 payload 迁移需求时，不得新增 Agent 输出 schema version。
#
# 新增 job/profile/output 类型必须优先进入集中注册表，注册表至少声明 job type、profile、
# prompt builder、DSPy Signature 和 OutputModel；不得再让这些信息散落在多个 if/else 分支。
#
# ## Agent 输出字段所有权
#
# 涉及 Agent、DSPy formatter、LLM 输出格式化或提示词输出要求时，计划必须先列字段所有权矩阵：
#
# - backend-owned：可由 job input、DB row、route 参数、store context、scheduler context
#   或确定性 projection 得到的字段，例如 job_id、batch_id、case ids、时间戳、scope、
#   provenance、eval run、gate result、task execution id。prompt 和 Signature 不得要求 LLM
#   输出这类字段；如外部脏输出包含这类字段，后端必须覆盖或忽略。
# - agent-owned：只有 Agent 结果才能提供的业务语义，例如分析结论、优化动作、业务假设、
#   风险说明、证据摘要和建议。这类字段才允许进入 Agent 输出契约。
# - boundary-owned：SQLite JSON、HTTP response、临时文件、日志、Langfuse metadata 等
#   序列化边界字段。`model_dump(mode="json")` 只应出现在这些边界。
#
# backend-owned 字段可以作为 formatter 输入上下文用于 grounding，但不能作为 Agent 输出结构的一部分。
# 如果为了兼容历史脏数据保留 normalizer，必须说明它只服务历史或外部边界，不能参与新成功主流程。
#
# ## 长程重构边界
#
# 长程重构、GSD 阶段执行或跨层 schema 收口必须先定义边界再实现。计划至少覆盖：
#
# - 内部 records、store 投影、API response schema、OpenAPI、前端生成类型、UI 使用、docs/tests。
# - 每个边界的单一真相来源，以及允许保留开放 JSON 的字段清单。
# - 公开 API、配置/env、Docker 路径、持久化数据和历史 payload 的删除、迁移或保留策略。
# - 旧 facade、shim、旧路径、旧工作流分支和不可达 UI 的删除条件。
#
# 持久化 row record、运行时投影 record 和 API response schema 不能因为字段相似就默认继承。
# 只有当生命周期约束、必填字段、历史数据兼容性和公开契约完全一致时，才允许共享同一模型；
# 否则应拆成语义清楚的模型，并通过共享字段类型或 base 模型减少重复。
#
# ## 生命周期状态完整性
#
# - 当字段表达持久化生命周期且存在受约束的合法转移时，必须进入集中状态机。
# - `status` / `state` / `phase` / `stage` 只是常见信号；只读外部字段、展示标签、临时 UI 步骤或无受限转移的简单属性，不强制状态机。
# - 集中状态机必须同时提供状态集合与完整合法转移表；缺转移表等同于未实现状态机。
# - 所有生命周期状态写入必须收口到调用状态转移校验的 helper；不得新增直写状态字段路径。
# - 新增状态值时，必须同步转移表、负向测试和相关文档。
#
# ## 优先治理边界
#
# - 新入口、路由、命令或 handler 优先放入对应职责模块，避免重新膨胀中心入口文件。
# - 新 job/profile/object/type 等分类值必须优先进入枚举、注册表或项目约定的集中定义。
# - 新 schema 字段必须以项目约定的模型、类型或 schema 为内部真相来源，持久化边界再做序列化和校验。
# - 发现旧 facade、兼容 shim、历史路径、旧数据格式、过期 API、不可达 UI 分支或调试脚本时，
#   优先给出删除、迁移或统一方案，而不是继续沿用旧入口。
# - 跨资源更新必须尽量放入同一事务或一致性边界；DB 与文件系统组合更新必须有幂等、回滚或部分失败处理。
# - 事务块内禁止执行不可回滚副作用，例如文件 `rmtree`、外部通知或远程调用。
# - 前端新增网络请求必须支持超时、取消或复用已有请求封装。
# - 修改公开 API、环境变量、Docker 默认值、agent profile 或 workspace 路径时，必须同步 README 或 docs 对应章节。
#
# ## 已知旧债
#
# 旧债由治理硬门对比 git base 自动识别：既有超限可暂存但不得增长，新增超限直接失败。
# 普通 docs 或 issue 可用于人工排期，但不得作为机器治理放行豁免。
