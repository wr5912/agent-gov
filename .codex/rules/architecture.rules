# 架构卫生规则
#
# 注意：`.rules` 文件会被 Codex 按 Starlark 解析。治理说明必须写成注释，
# 不要写 Markdown 正文，否则会触发 rules 加载失败。
#
# ## 上下文饱和检查
#
# 开始任何非琐碎代码变更前，必须确认待修改区域是否命中以下阈值：
#
# - 单个手写代码文件超过 800 行。Markdown 文档不适用该阈值。
# - 单类超过 30 个公开方法。
# - 单函数超过 80 行，或圈复杂度超过 15。
# - 路由文件超过 20 个路由。
# - 即将复制超过 40 行已有代码。
# - 同一职责在 3 个及以上文件中通过字符串字面量耦合。
# - 同一实体存在 Pydantic 模型与手写 dict 双轨表示。
# - `status` / `phase` / `stage` 等状态转移没有集中定义。
#
# 任一阈值命中时，计划中必须先说明拆分、抽象、注册表、状态机或 schema
# 单一来源的处理方式；不得以“匹配现有风格”为理由继续追加同类债务。
#
# ## 优先治理边界
#
# - 新路由优先放入对应 `app/routers/` 模块，避免重新膨胀 `app/main.py`。
# - 新 job/profile/object/status 类型必须优先进入枚举、注册表或集中状态机。
# - 新 schema 字段必须以 Pydantic 模型为内部真相来源，持久化边界再做 dump/validate。
# - 跨表更新必须尽量放入同一个 `Session.begin()`；DB 与文件系统组合更新必须说明幂等、回滚或部分失败处理。
# - 前端新增网络请求必须支持超时、取消或复用已有请求封装。
# - 修改公开 API、环境变量、Docker 默认值、agent profile 或 workspace 路径时，必须同步 README 或 docs 对应章节。
#
# ## 已知旧债
#
# 已超阈值文件记录在 `.codex/size-budget.yaml`。旧债允许逐步偿还，但不得增长；
# 新文件默认不允许加入预算豁免。
