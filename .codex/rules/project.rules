# 项目统一规则

本文件用于承载 Codex 项目级规则。规则来源于仓库 `docs/` 的项目基本设置要求，并吸收 `forrestchang/andrej-karpathy-skills` 中适合团队开发的行为准则。

## 行为准则

1. 三思而后行：先确认事实、假设、歧义和权衡，再进入实现。
2. 简洁优先：只为当前目标写必要代码，不做投机性功能或抽象。
3. 精准改动：每一处修改都必须服务于本次目标，避免无关重构和格式漂移。
4. 目标驱动：先定义成功标准，再用测试、构建或人工检查闭环验证。

## 执行规则

- 非琐碎代码变更必须遵循 Analyze -> Plan -> Execute -> Verify。
- 写文件前必须输出计划并等待用户确认。
- 计划必须包含涉及文件、逻辑修改点、四项准则对应、Docker 卷挂载路径和验证方式。
- 实现时优先复用项目已有模式、目录边界、工具链和错误处理方式。
- 不引入无明确当下价值的新依赖、新服务、新抽象或新配置。
- 发现无关问题时只记录和说明，不主动修改，除非用户要求。

## 环境规则

- Python 使用仓库本地 `.venv`，版本为 Python 3.10+。
- Python 包管理器使用 `uv`，禁止直接使用 `pip`。
- Node.js 使用 20+，包管理器优先使用 `pnpm`。
- Docker 文件放在 `docker/`，环境变量放在 `docker/.env`，卷挂载放在 `docker/volume/`。
- 容器内部端口小于 10000 时，宿主机端口映射为 `50000 + 原端口`。
- Dockerfile、CI 安装脚本和镜像构建脚本中的下载源优先使用国内源；例外必须写明原因。

## 安全规则

- 离线模式是产品不变量，必需工作流不得依赖远程服务。
- 日志、trace、diagnostics、导出包和 UI 不得暴露 API key、MCP header、数据库凭据和敏感工具输入。
- 不要硬编码配置值。
- 不要把个人路径、个人 token、账号信息、私有 registry 凭据写入团队共享配置。

## 验证规则

- 修 bug：优先写或运行能复现问题的测试，再证明修复有效。
- 新功能：验证核心成功路径、关键失败路径和必要边界条件。
- 重构：验证重构前后行为一致。
- 无法运行验证时：说明原因、残余风险和下一步验证方式。

## 外部实践保留说明

已保留 `andrej-karpathy-skills` 中对团队开发最有价值的内容：

- 显式管理假设和困惑。
- 抵制过度复杂化。
- 控制 diff 范围。
- 把任务转为可验证成功标准。

未保留内容：

- Claude Code 插件安装配置。
- Cursor 专用 `.cursor/rules` 配置。
- 个人技能目录安装说明。

原因：本分享包面向 Codex 项目级配置，保留跨工具通用的工程行为规则，避免混入与 Codex 无关或依赖个人环境的设置。
