# 验证规则
#
# 注意：`.rules` 文件会被 Codex 按 Starlark 解析。治理说明必须写成注释，
# 不要写 Markdown 正文，否则会触发 rules 加载失败。
#
# ## 功能验证
#
# - Python 运行使用仓库本地 `.venv/bin/python`。
# - 后端变更默认运行相关 `pytest`；跨模块变更运行 `make test` 或等价命令。
# - 前端变更默认运行 `pnpm build`，必要时补充浏览器验证。
# - Docker 持久化相关变更必须检查默认 `${HOME}/volume-agent-runtime` 路径，以及历史 `docker/volume/` 迁移/兼容路径。
#
# ## 架构验证
#
# 完成后必须说明：
#
# - 是否新增超过阈值的代码文件、函数、类或路由模块。
# - 是否新增超过 40 行的重复代码块。
# - 是否新增生命周期状态字面量分散、缺转移表的状态机、schema 双轨、跨事务中间态或硬编码分类字符串。
# - 如触发替换旧设计模式，是否验证旧 facade、兼容 shim、历史路径、重复实现、不可达分支或过期 API 已按计划删除、迁移或明确保留。
# - 如公开 API、配置/env、持久化数据或旧入口被改变，是否补充迁移说明、文档更新和针对新契约的测试。
# - 是否同步了 README、docs、生成类型、公开契约或环境变量示例。
# - 是否运行了项目覆盖层声明的治理硬门。
# - 如治理输出 `BASELINE` 或类似提示，是否确认它表示既有超限未增长；如输出 `FAIL`，必须先修复。
#
# ## 旧路径清零验证
#
# 触发替换旧设计模式后，Verify 阶段必须对旧 symbol、旧路由、旧 job_type、旧 prompt、旧 schema
# version、旧前端入口和旧生成类型做文本检索，并把结果分类：
#
# - 活跃运行时代码、路由、服务、前端交互或生成类型中的旧入口必须清零。
# - 负向测试、历史数据清理、只读迁移解释和文档中的旧名称可以保留，但必须说明用途。
# - 兼容点如果仍会被主流程调用，不能归类为历史兼容，必须回到替换旧设计计划继续清理。
#
# 修改公开 API 或前端契约后，必须验证 OpenAPI 和前端类型不再暴露已删除旧入口。
#
# ## 长程重构验证矩阵
#
# 长程重构、GSD 阶段执行、Pydantic/schema 收口、store 投影层变更、OpenAPI/前端类型变更，
# 默认必须覆盖以下验证；不适用时要在总结中说明原因：
#
# - fail 模式治理硬门。
# - 后端相关测试；跨模块或治理变更运行 `make test`。
# - OpenAPI 导出和前端生成类型同步。
# - 前端构建；涉及浏览器可见页面时补真实浏览器 smoke。
# - 涉及持久化投影、历史 schema 或 Docker 卷数据时，使用真实历史数据验证列表、详情和关键 UI。
# - 阶段结束时审计是否还有新增弱 `dict` / `JsonObject` 边界、schema 双轨、旧 shim 或生成物漂移。
#
# 非琐碎代码变更必须运行项目配置的 fail 模式治理检查。warn / dry-run 类模式只能用于
# Analyze 阶段观察，不能作为 Verify 通过标准。只有项目未配置硬门、环境缺失、依赖缺失
# 或脚本自身不可用时才可不运行，且必须把该验证项标记为未通过或未配置并说明后续恢复路径。
#
# ## 测试纵深
#
# - 生命周期状态字段变更：至少 1 个非法状态转移或非法输入测试。
# - 并发资源变更：至少 1 个重复执行、竞争或部分失败测试。
# - 外部输入变更：至少 1 个异常、恶意或越权输入测试。
# - 只有 happy path 时，必须说明剩余风险。
