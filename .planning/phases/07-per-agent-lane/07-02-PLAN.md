---
phase: 07-per-agent-lane
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - app/agent_testing/docker_engine.py
  - app/agent_testing/container_executor.py
  - app/agent_testing/worker.py
  - app/runtime/agent_git_commit_evidence.py
  - app/runtime/agent_git_environment.py
  - app/runtime/agent_repository_guard.py
  - app/runtime/recovery_cli_support.py
  - app/runtime/recovery_read_only_git.py
  - app/runtime/runtime_db.py
  - app/runtime/runtime_db_migrations_0057.py
  - app/runtime/runtime_db_migrations_0058.py
  - app/runtime/workspace_activation_recovery.py
  - app/runtime/workspace_activation_recovery_authority.py
  - app/runtime/workspace_activation_graph.py
  - app/runtime/service_launcher.py
  - app/services/agent_workspace_activation_recovery.py
  - app/services/agent_workspace_activation_recovery_preflight.py
  - app/services/agent_workspace_activation_recovery_wiring.py
  - Makefile
  - docker/agent-test-sandbox.Dockerfile
  - docker/docker-compose.yml
  - tests/test_agent_test_container_executor.py
  - tests/test_agent_test_worker.py
  - tests/test_repository_env_policy.py
  - tests/test_agent_git_authority.py
  - tests/test_runtime_db_0057.py
  - tests/test_runtime_db_0058.py
  - tests/test_workspace_activation_recovery_cli.py
  - tests/workspace_activation_recovery_test_support.py
autonomous: true
requirements: [LANE-02, LANE-03, LANE-04, LANE-08]

must_haves:
  truths:
    - "只有无 env_file、无网络的 agent-test-worker 挂 Docker socket；API 与 sandbox 均无 socket。"
    - "sandbox 使用 immutable image ID、隔离的 `python -I -P` 与 `--import-mode=importlib` pytest argv、65532:65532、network none、readonly root、cap drop ALL、no-new-privileges、固定 limits、同一 named volume 的唯一 `/workspace:ro` run subpath，以及 `/output`/`/tmp` 限额 tmpfs。"
    - "create 后 start 前由 Docker inspect 逐字段复核；计划参数、tag 或日志不能替代实际证据。"
    - "sandbox env 从空 allowlist 构建，不含 API/model/MCP secret；hostile tests 不能读 live data、socket、host path 或联网。"
    - "sandbox 源位于只挂给 worker 的 Docker-managed local named volume，API 不具备修改权限；volume driver/options、worker mountinfo backing、run root 与 subpath 任一漂移都在 start 前拒绝。"
    - "成功、pytest failure、timeout、cancel、materialize/inspect/ingest/cleanup error 与 worker restart 都清理 container/temp；cleanup failure 强制 error。"
    - "typed receipt 固定为 execution_provenance，Agent-owned pytest report 固定为 unverified diagnostics；不把 pytest 结果提升为独立业务正确性或真实 Agent 行为证明。"
    - "0057 operator recovery 只有本机只读 list/inspect、带 operation/recovery/state digest/operator/reason 的 exact apply，以及只按既有 reserved recovery ID 续跑的 resume；不暴露 force、HTTP/UI mutation 或任意 Git/SQLite 修复。"
    - "0057 completed/failed attempt 的 result/error 互斥且结构精确；空、冲突、额外字段、伪造类型和腐败持久化证据在 SQLite、投影与 resume 三层拒绝。"
    - "核心与 operator Git 命令共用固定 git-dir/work-tree authority，repository-local 外部驱动、对象图/作用域重定向、raw commit graph 改写、symbolic operation refs 和 HEAD 指向 activation namespace 全部 fail-closed。"
    - "0058 幂等重装 0055/0057 authority triggers，使已应用过旧版迁移的持久卷不会遗漏合法转移、不可变、禁止删除和终态证据约束。"
  artifacts:
    - path: app/agent_testing/container_executor.py
      provides: 固定 Docker sandbox create/inspect/run/bounded-ingest/remove contract
    - path: app/agent_testing/worker.py
      provides: durable claim、crash recovery、typed receipt terminal orchestration
    - path: docker/agent-test-sandbox.Dockerfile
      provides: 不含 Git/Docker/Node/Claude runtime 的最小 pytest 镜像
    - path: docker/docker-compose.yml
      provides: API/worker authority 与 env/socket/network 分离
    - path: app/runtime/runtime_db_migrations_0057.py
      provides: append-only operator recovery attempt、单 operation active owner 与严格终态迁移
    - path: app/runtime/recovery_read_only_git.py
      provides: 与核心 reconciler 复用平台自有 Git env/graph/index 语义，禁用继承 GIT_*、replace objects、repo/global/system config、hook、fsmonitor command、lazy fetch 和交互的只读观测边界
    - path: app/runtime/agent_git_environment.py
      provides: 绝对 Git executable、显式 git-dir/work-tree、repository config/path 准入与 canonical linked-worktree topology
    - path: app/runtime/agent_git_commit_evidence.py
      provides: 不经 graft/replace graph expansion 的 raw commit tree/parents 解析
    - path: app/runtime/runtime_db_migrations_0058.py
      provides: 存量卷 0055/0057 authority trigger 幂等重装
    - path: app/services/agent_workspace_activation_recovery.py
      provides: 稳定锁下预留/续跑 attempt、exact reconcile 与 strict-subset durable ref repair
    - path: app/services/agent_workspace_activation_recovery_preflight.py
      provides: 按 activation action/state 校验 audit、admission generation、HITL、active work 与 durable refs
    - path: Makefile
      provides: `make workspace-activation-recovery` 对 list/inspect/apply/resume 的唯一公共 Compose 入口
---

<objective>
实现独立 worker 和一次性最小权限 Docker sandbox，让不可信 Workspace pytest 的真实权限边界可由 inspect 与 cleanup 回执证明。
</objective>

<context>
@.planning/phases/07-per-agent-lane/07-CONTEXT.md
@.planning/phases/07-per-agent-lane/07-RESEARCH.md
@.planning/phases/07-per-agent-lane/07-01-SUMMARY.md
@docker/docker-compose.yml
@docker/Dockerfile
@app/runtime/service_launcher.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现最小 Docker Engine client 与 inspect-first executor</name>
  <files>app/agent_testing/docker_engine.py, app/agent_testing/container_executor.py, tests/test_agent_test_container_executor.py</files>
  <action>使用现有 httpx 的 UDS transport 封装所需 image/container/volume/log API。按 tag 解析 image ID，create body 只来自固定 contract；pytest argv 固定为 `/usr/local/bin/python -I -P -m pytest -q --import-mode=importlib -p agentgov_testkit.pytest_plugin tests`。start 前读取 inspect 并验证 image/argv/精确 env/user/network/rootfs/caps/security、同一 named volume identity 的唯一 `/workspace:ro` run subpath、`/output`/`/tmp` 限额 tmpfs、devices/privileged/limits。轮询 terminal/cancel/timeout，限长读取 multiplex logs；平台插件将同一份报告 JSON 写入 tmpfs 并以稳定单行 stdout envelope 发出，worker tail 读取、严格限长和 schema 校验，并从诊断 stdout 剔除 envelope。该通道不改变报告的 `agent_owned_unverified` 权威等级。最后显式 remove 并按 run label 验零残留。</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_agent_test_container_executor.py</automated></verify>
  <done>任何 inspect 偏差或 Docker 前置缺失 fail-closed，并执行可完成的清理。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 worker lifecycle 与所有终态 receipt</name>
  <files>app/agent_testing/worker.py, app/runtime/service_launcher.py, tests/test_agent_test_worker.py</files>
  <action>worker 启动先清理带 AgentGov test label 的遗留 container/temp 并将 running run 终结 interrupted；随后循环原子 claim queued。每轮在 worker-only `agent-test-runs` named volume 中重做 raw materialization/source/suite digest，执行前后都从实际 volume 目录安全快照，拒绝 live fixture，再调用 executor并在 cleanup 后一次写 terminal report/items/log/error/receipt。worker_id/container_id 只由 worker 写；并发/stale owner 不能覆写。worker inspect、volume inspect 与 mountinfo 必须共同证明 runs backing 和 API data bind 分离，错误配置启动即 fail-closed。</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_agent_test_worker.py tests/test_agent_testing.py -k 'worker or claim or cancel or interrupted or cleanup'</automated></verify>
  <done>API 与 worker 可独立重启，队列/取消/恢复有 durable owner，所有终态有可审计 cleanup。</done>
</task>

<task type="auto">
  <name>Task 3: 建立只读优先的 Workspace activation operator recovery</name>
  <files>app/runtime/agent_git_commit_evidence.py, app/runtime/agent_git_environment.py, app/runtime/agent_repository_guard.py, app/runtime/recovery_cli_support.py, app/runtime/recovery_read_only_git.py, app/runtime/runtime_db.py, app/runtime/runtime_db_migrations_0057.py, app/runtime/runtime_db_migrations_0058.py, app/runtime/workspace_activation_graph.py, app/runtime/workspace_activation_recovery.py, app/runtime/workspace_activation_recovery_authority.py, app/runtime/service_launcher.py, app/services/agent_workspace_activation_recovery.py, app/services/agent_workspace_activation_recovery_preflight.py, app/services/agent_workspace_activation_recovery_wiring.py, Makefile, tests/test_agent_git_authority.py, tests/test_runtime_db_0057.py, tests/test_runtime_db_0058.py, tests/test_workspace_activation_recovery_cli.py, tests/workspace_activation_recovery_test_support.py</files>
  <action>公共 `make workspace-activation-recovery` 只把 list/inspect/apply/resume 四个命令转发给受 runtime contract 保护的 service launcher。list/inspect 使用 read-only SQLite 和 hardened Git runner；核心与 operator 复用清除继承 `GIT_*`、禁用 replace/lazy fetch/hook 的 Git env、canonical graph 校验和完整 index flags 语义，且每条受管命令使用绝对 executable 与显式 `--git-dir`/`--work-tree`。仓库本地外部 filter/diff/merge/signing/credential 驱动、`core.worktree`、alternates、非规范 commondir、grafts 和 partial clone/promisor 均在命令前 fail-closed；commit tree/parents 从 raw object header 解析。operation refs 必须是精确 namespace 下的 direct refs，HEAD 只能 detached 或指向 `refs/heads/*`，ref 清理不能改变 HEAD topology。只输出脱敏指纹、计数、ref 名称、blocker 与状态摘要，不构造可写 Git store、不创建缺失 layout。inspect 每次生成 recovery_id 与 state_digest。apply 要求精确 operation/recovery/digest/operator/reason/action，先取得与平台 writer 相同的稳定 Agent 锁，再持久化单一 active append-only attempt 并重做 action/state-aware audit、admission generation、HITL、active work 与 ref 证据校验。`reconcile` 只调用 typed exact activation hook；`repair-missing-refs` 只补齐 journal 已记录对象且实际 refs 是严格子集的情形。apply 中断后只能按既有 reserved recovery ID 执行 resume，由 journal 复用原 action/digest/operator/reason，不重新接受这些字段。completed result 必须有精确四字段，failed error 只能有稳定 code；SQLite trigger、投影和 resume 全部 fail-closed。reserved attempt 排除 periodic writer，apply/resume、operator/periodic 的两种获锁顺序都不死锁且不允许旧摘要放行。0058 不建新表，只幂等重装 0055/0057 authority triggers，使已应用旧 migration 的卷也得到后续强化约束。不提供 force complete/reject、clear fence、任意 SHA/数据库路径或 HTTP/UI 写入。</action>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_agent_git_authority.py tests/test_runtime_db_0057.py tests/test_runtime_db_0058.py tests/test_workspace_activation_recovery_cli.py</automated></verify>
  <done>0057 fresh/0056-upgrade 幂等、0058 存量卷 trigger refresh、append-only 与 strict terminal evidence、reserve/started/terminal commit ack-loss、hostile inherited Git env/replace/fsmonitor/hook/filter/lazy-fetch 无执行、固定 git-dir/work-tree、repo-local 命令驱动/对象图重定向拒绝、raw commit graph 与 direct-ref/HEAD topology、canonical graph/index、state-aware preflight、stale digest、缺失/冲突 refs、两种 operator/periodic 与 apply/resume 竞态、reserved resume、四命令 launcher 分发、缺失 layout 与无 force CLI 均有行为测试。</done>
</task>

<task type="auto">
  <name>Task 4: 部署专用 worker 与最小 sandbox 镜像</name>
  <files>docker/agent-test-sandbox.Dockerfile, docker/docker-compose.yml, tests/test_repository_env_policy.py</files>
  <action>新增最小 Python/pytest/testkit sandbox image，不安装 Git、Docker CLI、Node 或 Claude runtime。Compose worker 使用 API image/control code 但无 env_file/network，只显式接收共享 data root、Docker-managed local `agent-test-runs` volume identity、sandbox image、timeout 和 socket；不传 host runs path。worker 用 Docker volume inspect 与自身 mountinfo 共同证明 named-volume backing、driver/options、run root 和 API data bind 分离；API 无 socket且不挂载该 named volume。worker 是默认 core service并在 API runtime/data 就绪后启动。</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_repository_env_policy.py -k 'agent_test_worker or sandbox or docker_socket'</automated></verify>
  <done>Compose 静态契约证明 secret/socket/network/volume authority 分离，sandbox image attack surface 最小。</done>
</task>

</tasks>

<verification>
目标 pytest、0057/0058 migration 重复执行与 commit ack-loss、Git authority hostile config/path/ref 证据、CLI 只读/apply/resume/无 force 契约、compose config sanitized inspection、ruff、pyright；不得用 mock-only 结论声明真实容器完成。
</verification>

<output>Create `07-02-SUMMARY.md`。</output>
