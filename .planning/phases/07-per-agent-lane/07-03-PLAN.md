---
phase: 07-per-agent-lane
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - scripts/test_quality/models.py
  - scripts/test_quality/policy.py
  - tests/quality_policy.json
  - scripts/run_agent_test_container_e2e.py
  - scripts/run_container_acceptance.py
  - .codex/hooks/container_acceptance_guard.py
  - Makefile
  - app/openapi_contract.py
  - frontend/src/types/api.ts
  - frontend/src/components/ReleaseWorkbench.tsx
  - tests/test_test_quality_policy.py
  - tests/test_runtime_container_acceptance.py
  - tests/test_openapi_export.py
  - docs/AgentGov集成指南.md
  - docs/反馈闭环当前实现基线.md
  - docs/engineering/AgentGov下一阶段P0准入收口实施方案.md
autonomous: true
requirements: [LANE-05, LANE-06, LANE-07, LANE-08]

must_haves:
  truths:
    - "quality policy 显式登记 P0 exact-commit、P0-MCP、P1 live 三条 lane 的 owner/capabilities/resources/enforcement，Workspace leaf 仍不进入根 collection。"
    - "OpenAPI 要求手工 run 的完整 commit 并公开 nullable typed receipt；前端类型由 schema 生成，发布 UI 不再把 status-only 当作门禁。"
    - "公共 make lane 从当前工作树构建 worker/sandbox image，在独立临时 runtime root 跑真实安全 Agent exact commit 和 hostile isolation cases。"
    - "Docker/镜像/inspect/cleanup 前置缺失返回非零而非 skip，任意运行结束无带 label container 或临时目录。"
    - "文档明确历史回执、静态与 live lane、worker authority、真实容器入口和长期 P1 seam，不保留‘无迁移/API变化’陈述。"
  artifacts:
    - path: tests/quality_policy.json
      provides: 三类阶段 lane 单一分类入口
    - path: scripts/run_agent_test_container_e2e.py
      provides: 当前工作树 exact-commit + hostile sandbox 真实回执
    - path: Makefile
      provides: container-workspace-pytest-test 公共入口
    - path: frontend/src/types/api.ts
      provides: 由 OpenAPI 生成的 typed receipt client contract
---

<objective>
把隔离实现接入测试组合、公开契约、UI、文档与真实 Docker acceptance，使 Phase 7 能以当前候选的机器证据退出，而不是停在单测或 Compose 设计。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 扩展质量策略并更新 OpenAPI/前端门禁</name>
  <files>scripts/test_quality/models.py, scripts/test_quality/policy.py, tests/quality_policy.json, app/openapi_contract.py, frontend/src/types/api.ts, frontend/src/components/ReleaseWorkbench.tsx, tests/test_test_quality_policy.py, tests/test_openapi_export.py</files>
  <action>Lane model 增加 owner/capabilities/resources/entrypoint/collection boundary，并校验引用。登记三条 lane，P0 exact-commit 为 blocking且指向公共 Make 入口；P0-MCP/P1 live 保持各自独立准入，不冒充已实现。更新 main-flow 旧 runner/restart/status-only描述与 nodeids。生成 OpenAPI 类型，UI只展示后端 publication blocker/receipt eligibility。</action>
  <verify><automated>.venv/bin/python scripts/check_test_quality_policy.py --manifest-only --policy tests/quality_policy.json</automated><automated>make openapi-contract-check openapi-type-drift-check ui-build</automated></verify>
  <done>LANE-05/06/07 有机器可读分类和公开 typed contract，前端不复制安全门逻辑。</done>
</task>

<task type="auto">
  <name>Task 2: 建立真实容器 exact-commit 与 hostile acceptance</name>
  <files>scripts/run_agent_test_container_e2e.py, scripts/run_container_acceptance.py, .codex/hooks/container_acceptance_guard.py, Makefile, tests/test_runtime_container_acceptance.py</files>
  <action>公共入口使用独立 Compose project/临时 runtime root/当前工作树 build，启动 API+worker并构建 sandbox；通过 API取得当前安全 Agent commit并显式创建 run，验证 58-leaf 当前 suite全绿但不固定数量。另以临时 hostile Agent commit 验证 secret env、live data、socket、host escape与联网均失败。成功/pytest失败/cancel/timeout均核对 receipt与零残留。guard 阻止绕过 freshness入口直接调用私有脚本。</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_runtime_container_acceptance.py</automated><automated>make container-workspace-pytest-test</automated></verify>
  <done>真实 Docker lane在当前候选生成 source/image/commit/digest/isolation/cleanup 回执；缺失前置严格失败。</done>
</task>

<task type="auto">
  <name>Task 3: 同步产品/运维文档并完成阶段全量门</name>
  <files>docs/AgentGov集成指南.md, docs/反馈闭环当前实现基线.md, docs/engineering/AgentGov下一阶段P0准入收口实施方案.md</files>
  <action>同步 validate-and-enqueue、worker/sandbox、完整 commit请求、static/live边界、typed receipt、历史不可放行、公共真实容器入口与回退方式；修订 P0 文档中“无数据库/OpenAPI变化”的短期陈述。随后运行治理、类型、主流程、串行全量测试并形成 Phase verification。</action>
  <verify><automated>make codex-guard</automated><automated>make typecheck</automated><automated>make main-flow-test</automated><automated>make test</automated></verify>
  <done>实现、契约、UI、质量策略和文档同一真相，所有 Phase 7 门通过。</done>
</task>

</tasks>

<verification>
1. 目标 unit/component/migration/OpenAPI/policy 测试。
2. `make container-workspace-pytest-test` 当前工作树真实 Docker 回执。
3. `make codex-guard`、`make typecheck`、`make main-flow-test`、串行 `make test`。
4. 复核 API 无 socket、worker无 env_file/network、sandbox 无 `/data`/socket/secret、零 label/temp残留。
</verification>

<output>Create `07-03-SUMMARY.md` and `07-VERIFICATION.md`。</output>
