services:
  claude-agent-api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APT_MIRROR: ${APT_MIRROR:-https://mirrors.aliyun.com/debian}
        APT_SECURITY_MIRROR: ${APT_SECURITY_MIRROR:-https://mirrors.aliyun.com/debian-security}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://mirrors.aliyun.com/pypi/simple/}
        PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-mirrors.aliyun.com}
        NPM_REGISTRY: ${NPM_REGISTRY:-https://registry.npmmirror.com}
    image: claude-agent-runtime-api:0.1.0
    container_name: claude-agent-runtime-api
    ports:
      - "${HOST_PORT:-8080}:${API_PORT:-8080}"
    env_file:
      - .env
    environment:
      API_HOST: ${API_HOST:-0.0.0.0}
      API_PORT: ${API_PORT:-8080}
      HOST_PORT: ${HOST_PORT:-8080}
      HOST_WORKSPACE_MOUNT: ${HOST_WORKSPACE_MOUNT:-./workspace}
      HOST_DATA_MOUNT: ${HOST_DATA_MOUNT:-./data}
      HOST_CLAUDE_ROOT_MOUNT: ${HOST_CLAUDE_ROOT_MOUNT:-./claude-root}
      WORKSPACE_DIR: ${WORKSPACE_DIR:-/workspace}
      DATA_DIR: ${DATA_DIR:-/data}
      CLAUDE_HOME: ${CLAUDE_HOME:-/root/.claude}
    volumes:
      - ${HOST_WORKSPACE_MOUNT:-./workspace}:${WORKSPACE_DIR:-/workspace}
      - ${HOST_DATA_MOUNT:-./data}:${DATA_DIR:-/data}
      - ${HOST_CLAUDE_ROOT_MOUNT:-./claude-root}:/root
    restart: unless-stopped
