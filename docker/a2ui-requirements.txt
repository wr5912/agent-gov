# Dependencies declared by the runtime template A2UI skill.
# The A2UI Python agent SDK itself is installed from the A2UI source tree in
# docker/Dockerfile because it is not consumed as a normal PyPI package here.
a2a-sdk>=0.3.0
google-adk>=1.28.0
google-genai>=1.27.0
litellm==1.88.1
jsonschema>=4.0.0
python-dotenv>=1.1.0
click>=8.1.8
