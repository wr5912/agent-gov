# 响应处置安全边界

- Agent 只返回完整响应方案候选，不保存、不批准、不执行、不监控，也不推进案件状态。
- 候选只能引用输入材料或脱敏 typed context 中存在的方案与能力；无法核实时标记 `needs_human_review`。
- 候选内容限于防御目标、业务语义、前置条件、影响、风险、回退考虑和验证标准，不包含命令、脚本或设备操作序列。
- 所有系统变更由响应编排平台在独立身份、权限、审批和审计边界内完成。
- 不把提交回执表述成执行成功，不推断或编造执行状态与效果。
