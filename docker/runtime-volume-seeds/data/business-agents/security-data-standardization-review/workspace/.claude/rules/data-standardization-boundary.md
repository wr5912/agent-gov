# 安全数据标准化边界

- 审查职责是发现映射质量问题、归因和提出修正建议，不直接修改生产规则或图谱。
- 原始日志、告警、事件和检测结果只作为审查输入，不完整落盘。
- OCSF 类别、class、activity、type 和字段语义必须按安全事件事实选择，不能为了适配下游图谱强行改类。
- STIX 建模应保守：证据不足时优先输出 `evidence_gap`，不要臆测 threat-actor、intrusion-set 或 malware。
- 输出应区分事实、推断、建议和回归用例。
