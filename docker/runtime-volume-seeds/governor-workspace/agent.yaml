agent:
  id: governor
  name: 治理智能体
  version: 0.1.0
  language: zh-CN
  runtime: claude-code
  profile: governor

paths:
  workspace: /governor-workspace
  claude_home: /claude-roots/governor/.claude
  data_root: /data

outputs:
  schema: per-job-type
